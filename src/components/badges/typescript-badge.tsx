import { Badge } from '@/ui/badge';

export const TypeScriptBadge = () => {
  return (
    <Badge variant="default" className="mr-2 mb-2 bg-blue-500 text-white">
      TypeScript
    </Badge>
  );
};
